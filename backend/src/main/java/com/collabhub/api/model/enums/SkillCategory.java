package com.collabhub.api.model.enums;

/**
 * Categories for grouping skills in the UI.
 * Must match the ENUM values in the `skill` table's `category` column.
 */
public enum SkillCategory {
    LANGUAGE,
    FRAMEWORK,
    TOOL,
    CONCEPT,
    OTHER
}
